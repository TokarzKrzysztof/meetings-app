import { useState } from 'react';

export async function loader() {
  return null;
}

export const Login = () => {
  const [a, s] = useState();

  return <>
  
  </>;
};

Login.displayName = 'Login';
